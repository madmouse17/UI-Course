import 'package:flutter/material.dart';
import 'package:get/get.dart';

var mediaHeight = MediaQuery.of(Get.context!).size.height;
var mediaWidth = MediaQuery.of(Get.context!).size.width;

var themeText = Theme.of(Get.context!).textTheme;
